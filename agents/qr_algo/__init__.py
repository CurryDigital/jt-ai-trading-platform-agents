"""qr_algo — backtest execution agent.

Public surface:
    AlgoAgent  — the event-driven worker (algo_agent.py)
    backtest   — pure-stdlib backtest engine
    strategies — strategy_type → trade-generator dispatch
"""
